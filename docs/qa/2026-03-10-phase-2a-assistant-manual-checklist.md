# Phase 2A Assistant Manual Checklist

- Read page (`/read/tech/p_garden/why-3d-galaxy`): ask `总结当前页面`.
- Planet page: ask `这个星球主要讲什么`.
- Hub page (`/`): ask `这个花园主要有哪些内容` and confirm the answer gives a whole-garden overview.
- Hub page (`/`): ask `我是第一次来，怎么逛比较合适` and confirm the answer acts as a first-visit guide.
- Read page (`/read/tech/p_garden/why-3d-galaxy`): ask `这个花园主要有哪些内容` and confirm the answer stays at whole-garden scope instead of collapsing to only the current article.
- Hub page (`/`): ask `总结当前页面` and confirm the fallback explains that the user is on the home hub.
